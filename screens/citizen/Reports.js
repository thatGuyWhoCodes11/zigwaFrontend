//don't have enough time to make reports tab in database, for now it'll be stored inside of the device as a prototype demonstration of work
import { Text } from "react-native";

export default function Reports(){
    <Text>this is reports tab</Text>
}